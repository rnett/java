// Targeted by JavaCPP version 1.5.4: DO NOT EDIT THIS FILE

package org.tensorflow.internal.c_api;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.tensorflow.internal.c_api.global.tensorflow.*;


// Vector space for actually computing gradients. Implements methods for calling
// the backward function with incoming gradients and returning the outgoing
// gradient and for performing gradient aggregation.
// See `tensorflow::eager::VSpace` for more details.
@Namespace("tensorflow::gradients") @NoOffset @Properties(inherit = org.tensorflow.internal.c_api.presets.tensorflow.class)
public class TapeVSpace extends org.tensorflow.internal.c_api.AbstractTapeVSpace {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public TapeVSpace(Pointer p) { super(p); }

  public TapeVSpace(TFE_Context ctx) { super((Pointer)null); allocate(ctx); }
  private native void allocate(TFE_Context ctx);

  // Returns the number of elements in the gradient tensor.
  public native long NumElements(TFE_TensorHandle tensor);

  // Consumes references to the tensors in the gradient_tensors list and returns
  // a tensor with the result.
  public native TFE_TensorHandle AggregateGradients(
        @ByVal HandleList gradient_tensors);

  // Calls the passed-in backward function.
  public native @ByVal TF_Status CallBackwardFunction(
        BackwardFunction backward_function,
        @StdVector long unneeded_gradients,
        @ByVal HandleList output_gradients,
        @Cast("AbstractTensorHandle**") @StdVector PointerPointer result);
  public native @ByVal TF_Status CallBackwardFunction(
        BackwardFunction backward_function,
        @StdVector long unneeded_gradients,
        @ByVal HandleList output_gradients,
        @StdVector @ByPtrPtr TFE_TensorHandle result);

  // Builds a tensor filled with ones with the same shape and dtype as `t`.
  public native @ByVal TF_Status BuildOnesLike(@Const @ByRef TapeTensor t,
                         @Cast("AbstractTensorHandle**") PointerPointer result);
  public native @ByVal TF_Status BuildOnesLike(@Const @ByRef TapeTensor t,
                         @ByPtrPtr TFE_TensorHandle result);

  // Looks up the ID of a Gradient.
  public native long TensorId(TFE_TensorHandle tensor);

  // Converts a Gradient to a TapeTensor.
  public native @ByVal TapeTensor TapeTensorFromGradient(TFE_TensorHandle g);

  public native void MarkAsResult(TFE_TensorHandle gradient);

  public native void DeleteGradient(TFE_TensorHandle gradient);
}
